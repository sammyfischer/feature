{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "ProjectConfig",
  "type": "object",
  "properties": {
    "branch": {
      "description": "Branch name options",
      "$ref": "#/$defs/BranchConfig",
      "default": {
        "sep": "-"
      }
    },
    "default_remote": {
      "description": "Name of the remote to use when one can't be determined automatically",
      "type": "string",
      "default": "origin"
    },
    "projects": {
      "description": "List of subprojects",
      "type": "object",
      "additionalProperties": {
        "$ref": "#/$defs/ProjectEntry"
      },
      "default": {}
    },
    "protect": {
      "description": "List of branches to protect from force-pushes/deletion",
      "type": "array",
      "default": [
        "main"
      ],
      "items": {
        "type": "string"
      }
    },
    "tag": {
      "description": "Semver tag options",
      "$ref": "#/$defs/TagConfig",
      "default": {
        "require_annotated": false
      }
    }
  },
  "$defs": {
    "BranchConfig": {
      "type": "object",
      "properties": {
        "sep": {
          "description": "Separator used between words in branch names",
          "type": "string",
          "default": "-"
        },
        "template": {
          "description": "Template for creating branch names. See `feature start --help` for more\ninfo",
          "type": [
            "string",
            "null"
          ]
        }
      }
    },
    "ProjectEntry": {
      "type": "object",
      "properties": {
        "path": {
          "type": "string"
        },
        "url": {
          "type": "string"
        }
      },
      "required": [
        "url",
        "path"
      ]
    },
    "TagConfig": {
      "type": "object",
      "properties": {
        "require_annotated": {
          "description": "Require semver tags to be annotated",
          "type": "boolean"
        }
      },
      "required": [
        "require_annotated"
      ]
    }
  }
}
